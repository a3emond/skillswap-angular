import { Routes } from '@angular/router';
import { SignupComponent } from './core/signup.component/signup.component';

export const routes: Routes = [

{
  path: 'signup',
  component: SignupComponent
}

];
